import 'package:flutter/material.dart';

class LerneinheitScreen extends StatelessWidget {
  static const String id = 'LerneinheitPage';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Welcome in LerneinheitPage'),
      ),
    );
  }
}
