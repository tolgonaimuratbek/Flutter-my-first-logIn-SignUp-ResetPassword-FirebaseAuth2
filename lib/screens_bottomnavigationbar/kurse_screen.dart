import 'package:flutter/material.dart';

class KurseScreen extends StatelessWidget {
  static const String id = 'KursePage';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Welcome in KursePage'),
      ),
    );
  }
}
