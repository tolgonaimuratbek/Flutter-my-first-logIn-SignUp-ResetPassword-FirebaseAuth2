import 'package:flutter/material.dart';

class ZuletztScreen extends StatelessWidget {
  static const String id = 'ZuletztPage';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Welcome in ZuletztPage'),
      ),
    );
  }
}
